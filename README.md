# iOS Controller

A remote-control app for the TV-HOME project.

Target device: iPhone 5 (iOS 10.3.4).

The project is intended to be built with a legacy-compatible iOS toolchain and used as a simple LAN remote for TV-HOME.
